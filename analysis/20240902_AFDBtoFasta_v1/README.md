# Extract protein sequences from the AFDB

## Workflow
- `python bin/extractFastaFromAFDB.py` after having specified a temporary directory and a glob pattern to AFDB structures in ".pdb" format (requires AFDB database);